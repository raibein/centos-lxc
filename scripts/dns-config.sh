# Get the information of the host
hostnamectl

# Install DNS Server Software :
# To install the DNS server software, using the Bind (Berkeley Internet Name Domain) 
# which is a widely used and highly reliable DNS server software.
sudo apt-get install bind9


# Configure Bind DNS Server :
